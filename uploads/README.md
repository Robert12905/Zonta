This folder is used by the CMS to store uploaded media (images). Place your default hero image here as `default-hero.jpg` or else update `content/homepage.json` with another filename.

Example: uploads/default-hero.jpg
